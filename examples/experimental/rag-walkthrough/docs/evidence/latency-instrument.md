# query_latency 的儀器確定性

## 量到什麼

同一份設定、同一批查詢，在同一台機器連跑五次，取 p50 延遲。

- 五次的 p50：176、178、181、177、179 毫秒
- 全距 5 毫秒，相對全距 2.8%

## 為什麼這支撐得起中止門檻

Contract 的中止門檻是 p50 超過 400 毫秒。儀器本身的擺動只有 5 毫秒，離門檻兩個數量級，所以觸發中止一定是真的變慢，不是量測噪音。

## 什麼時候要重做

換硬體、換 runtime 版本，或改動 batch 設定時，這份量測作廢——這三項也是 run 設定快照必須記下來的原因。
